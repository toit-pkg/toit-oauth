name: oauth
description: OAuth 2.0 authentication client supporting localhost code flow and device flow.
dependencies:
  host:
    url: github.com/toitlang/pkg-host
    version: ^1.15.3
  http:
    url: github.com/toitlang/pkg-http
    version: ^2.8.0
